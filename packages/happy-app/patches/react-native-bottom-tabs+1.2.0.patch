diff --git a/node_modules/react-native-bottom-tabs/ios/Extensions.swift b/node_modules/react-native-bottom-tabs/ios/Extensions.swift
index fed3dd2..447643e 100644
--- a/node_modules/react-native-bottom-tabs/ios/Extensions.swift
+++ b/node_modules/react-native-bottom-tabs/ios/Extensions.swift
@@ -53,6 +53,47 @@ extension PlatformImage {
   }
 }
 
+#if os(iOS)
+extension UIImage {
+  /// The color the system itself draws tab bar items with on this OS version.
+  /// iOS 26 tab bars ignore the per-item tint colors and paint every item with a
+  /// vibrant label color, so an icon whose dot badge is baked into the bitmap has to
+  /// use that same color to keep looking like its neighbours. Older systems respect
+  /// the tint colors, so there the app provided tint stays in charge.
+  static func systemTabItemTint(for colorScheme: ColorScheme) -> UIColor? {
+    if #available(iOS 26.0, *) {
+      let style: UIUserInterfaceStyle = colorScheme == .dark ? .dark : .light
+      return UIColor.label
+        .resolvedColor(with: UITraitCollection(userInterfaceStyle: style))
+        .withAlphaComponent(0.9)
+    }
+    return nil
+  }
+
+  func addingDotBadge(tintColor: UIColor?, colorScheme: ColorScheme) -> UIImage {
+    let verticalInset: CGFloat = 6
+    let horizontalInset: CGFloat = 6
+    let canvasSize = CGSize(
+      width: size.width + horizontalInset * 2,
+      height: size.height + verticalInset * 2
+    )
+    let renderer = UIGraphicsImageRenderer(size: canvasSize)
+    let rendered = renderer.image { _ in
+      let baseImage = (UIImage.systemTabItemTint(for: colorScheme) ?? tintColor).map {
+        withTintColor($0, renderingMode: .alwaysOriginal)
+      } ?? self
+      baseImage.draw(at: CGPoint(x: horizontalInset, y: verticalInset))
+
+      UIColor.systemRed.setFill()
+      UIBezierPath(
+        ovalIn: CGRect(x: horizontalInset + size.width - 2, y: 0, width: 8, height: 8)
+      ).fill()
+    }
+    return rendered.withRenderingMode(.alwaysOriginal)
+  }
+}
+#endif
+
 extension View {
 #if os(macOS)
   @MainActor
diff --git a/node_modules/react-native-bottom-tabs/ios/TabView/LegacyTabView.swift b/node_modules/react-native-bottom-tabs/ios/TabView/LegacyTabView.swift
index 5bfc58f..04d15e2 100644
--- a/node_modules/react-native-bottom-tabs/ios/TabView/LegacyTabView.swift
+++ b/node_modules/react-native-bottom-tabs/ios/TabView/LegacyTabView.swift
@@ -3,6 +3,9 @@ import SwiftUI
 struct LegacyTabView: AnyTabView {
   @ObservedObject var props: TabViewProps
 
+  /// Keeps the baked dot badge icon in sync with the appearance.
+  @Environment(\.colorScheme) private var colorScheme
+
   var onLayout: (CGSize) -> Void
   var onSelect: (String) -> Void
   var updateTabBarAppearance: () -> Void
@@ -39,7 +42,18 @@ struct LegacyTabView: AnyTabView {
       let isFocused = props.selectedPage == tabData.key
 
       if !tabData.hidden || isFocused {
+        #if os(iOS)
+        let icon = tabData.badge == " "
+          ? props.icons[index]?.addingDotBadge(
+              tintColor: isFocused
+                ? (tabData.activeTintColor ?? props.activeTintColor)
+                : props.inactiveTintColor,
+              colorScheme: colorScheme
+            )
+          : props.icons[index]
+        #else
         let icon = props.icons[index]
+        #endif
         let child = props.children[safe: index]?.view ?? PlatformView()
         let context = TabAppearContext(
           index: index,
@@ -61,7 +75,7 @@ struct LegacyTabView: AnyTabView {
             .accessibilityIdentifier(tabData.testID ?? "")
           }
           .tag(tabData.key)
-          .tabBadge(tabData.badge)
+          .tabBadge(tabData.badge == " " ? nil : tabData.badge)
           .tabAppear(using: context)
       }
     }
diff --git a/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift b/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift
index 22c52cd..8a3170d 100644
--- a/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift
+++ b/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift
@@ -5,6 +5,9 @@ import SwiftUI
 struct NewTabView: AnyTabView {
   @ObservedObject var props: TabViewProps
 
+  /// Keeps the baked dot badge icon in sync with the appearance.
+  @Environment(\.colorScheme) private var colorScheme
+
   var onLayout: (CGSize) -> Void
   var onSelect: (String) -> Void
   var updateTabBarAppearance: () -> Void
@@ -28,7 +31,18 @@ struct NewTabView: AnyTabView {
           let isFocused = props.selectedPage == tabData.key
 
           if !tabData.hidden || isFocused {
+            #if os(iOS)
+            let icon = tabData.badge == " "
+              ? props.icons[index]?.addingDotBadge(
+                  tintColor: isFocused
+                    ? (tabData.activeTintColor ?? props.activeTintColor)
+                    : props.inactiveTintColor,
+                  colorScheme: colorScheme
+                )
+              : props.icons[index]
+            #else
             let icon = props.icons[index]
+            #endif
 
             let context = TabAppearContext(
               index: index,
@@ -52,7 +66,7 @@ struct NewTabView: AnyTabView {
               )
             }
             #if !os(tvOS)
-            .badge(tabData.badge.flatMap { !$0.isEmpty ? Text($0) : nil })
+            .badge(tabData.badge.flatMap { $0 == " " ? nil : Text($0) })
             #endif
             .accessibilityIdentifier(tabData.testID ?? "")
           }
